# 量子态层析重构GUI设计方案 v2.0

## 🎨 重新设计的专业GUI布局方案

### **🏗️ 五页面主窗口架构**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│ 菜单栏: 文件 | 编辑 | 视图 | 工具 | 分析 | 帮助                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│ 工具栏: [新建] [打开] [保存] [运行] [停止] [设置] [导出] [帮助] [全屏]           │
├─────────────────────────────────────────────────────────────────────────────────┤
│ 主工作区 (QStackedWidget - 五页面切换)                                          │
│ ┌─────────────────────────────────────────────────────────────────────────────┐ │
│ │ 页面1: 数据准备 (Data Preparation)                                        │ │
│ │ ┌─────────────────────┬─────────────────────────────────────────────────┐   │ │
│ │ │ 左侧: 数据输入面板   │ 右侧: 数据预览区域                             │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ 输入方式选择     │ │ │ 数据预览表格                               │ │   │ │
│ │ │ │ ○ 文件选择      │ │ │ - 每列: 样本编号                           │ │   │ │
│ │ │ │ ○ 自定义输入    │ │ │ - 每行: 测量基编号                         │ │   │ │
│ │ │ └─────────────────┘ │ │ - 数据值显示                               │ │   │ │
│ │ │ ┌─────────────────┐ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ │ 文件选择区域     │ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ - 文件管理器    │ │ │ 预览控制区域                               │ │   │ │
│ │ │ │ - 支持格式:     │ │ │ - [数据预览] 按钮                          │ │   │ │
│ │ │ │   Excel/CSV/    │ │ │ - 预览状态显示                             │ │   │ │
│ │ │ │   JSON/HDF5     │ │ │ - 数据统计信息                             │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ ┌─────────────────┐ │                                                 │   │ │
│ │ │ │ 自定义输入区域   │ │                                                 │   │ │
│ │ │ │ - Excel编辑器   │ │                                                 │   │ │
│ │ │ │ - 维度设置      │ │                                                 │   │ │
│ │ │ │ - 数据输入      │ │                                                 │   │ │
│ │ │ └─────────────────┘ │                                                 │   │ │
│ │ └─────────────────────┴─────────────────────────────────────────────────┘   │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────────────────────┐ │
│ │ 页面2: 参数配置与执行监控 (Configuration & Execution Monitoring)          │ │
│ │ ┌─────────────────────┬─────────────────────────────────────────────────┐   │ │
│ │ │ 左侧: 参数配置面板   │ 右侧: 执行监控面板                             │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ 基本参数配置     │ │ │ 执行控制区域                               │ │   │ │
│ │ │ │ - 系统维度      │ │ │ - [开始重构] 按钮                          │ │   │ │
│ │ │ │   (自定义输入)   │ │ │ - [暂停/恢复] 按钮                        │ │   │ │
│ │ │ │ - 重构方法      │ │ │ - [停止重构] 按钮                          │ │   │ │
│ │ │ │   (Linear/MLE/  │ │ │ - [取消操作] 按钮                          │ │   │ │
│ │ │ │   Both)         │ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ └─────────────────┘ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ ┌─────────────────┐ │ │ 监控面板区域                               │ │   │ │
│ │ │ │ 高级参数配置     │ │ │ - 整体进度条                               │ │   │ │
│ │ │ │ - 数值容差      │ │ │ - 当前阶段显示                             │ │   │ │
│ │ │ │ - 迭代次数      │ │ │ - 样本进度显示                             │ │   │ │
│ │ │ │ - 正则化参数    │ │ │ - 时间估算显示                             │ │   │ │
│ │ │ │ - 投影算子缓存  │ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ └─────────────────┘ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ ┌─────────────────┐ │ │ 实时日志区域                               │ │   │ │
│ │ │ │ 输出设置         │ │ │ - 执行日志                                 │ │   │ │
│ │ │ │ - 输出目录      │ │ │ - 警告信息                                 │ │   │ │
│ │ │ │ - 文件格式      │ │ │ - 调试信息                                 │ │   │ │
│ │ │ │ - 自动保存      │ │ │ - 错误信息                                 │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ └─────────────────────┴─────────────────────────────────────────────────┘   │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────────────────────┐ │
│ │ 页面3: 结果可视化与基础分析 (Results Visualization & Basic Analysis)       │ │
│ │ ┌─────────────────────┬─────────────────────────────────────────────────┐   │ │
│ │ │ 左侧: 结果选择面板   │ 右侧: 可视化展示区域                           │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ 结果列表区域     │ │ │ 可视化标签页                               │ │   │ │
│ │ │ │ - 样本选择      │ │ │ - 密度矩阵热图                             │ │   │ │
│ │ │ │ - 方法选择      │ │ │ - 幅度相位3D图                             │ │   │ │
│ │ │ │ - 时间排序      │ │ │ - 实部虚部3D图                             │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ 基础指标显示     │ │ │ 分析图表标签页                             │ │   │ │
│ │ │ │ - 纯度指标      │ │ │ - 指标趋势图                               │ │   │ │
│ │ │ │ - 迹指标        │ │ │ - 方法对比图                               │ │   │ │
│ │ │ │ - 矩阵秩        │ │ │ - 统计分布图                               │ │   │ │
│ │ │ │ - 条件数        │ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ └─────────────────┘ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ ┌─────────────────┐ │ │ 保真度分析区域                             │ │   │ │
│ │ │ │ 保真度分析       │ │ │ - [选择参考密度矩阵] 按钮                  │ │   │ │
│ │ │ │ - [选择参考文件] │ │ │ - 参考文件路径显示                         │ │   │ │
│ │ │ │ - [开始分析]     │ │ │ - 保真度值显示                             │ │   │ │
│ │ │ │ - 保真度值显示   │ │ │ - 分析状态显示                             │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ └─────────────────────┴─────────────────────────────────────────────────┘   │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────────────────────┐ │
│ │ 页面4: 高级分析功能 (Advanced Analysis Functions)                          │ │
│ │ ┌─────────────────────┬─────────────────────────────────────────────────┐   │ │
│ │ │ 左侧: 分析配置面板   │ 右侧: 高级分析区域                             │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ Bell态分析配置   │ │ │ Bell态分析区域                             │ │   │ │
│ │ │ │ - Bell态分析开关│ │ │ - Bell态保真度计算                         │ │   │ │
│ │ │ │ - 分析参数设置  │ │ │ - Bell态可视化                             │ │   │ │
│ │ │ │ - 输出格式设置  │ │ │ - 纠缠度分析                               │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ 统计分析方法     │ │ │ 统计分析区域                               │ │   │ │
│ │ │ │ - 描述性统计    │ │ │ - 描述性统计结果                           │ │   │ │
│ │ │ │ - 假设检验      │ │ │ - 假设检验结果                             │ │   │ │
│ │ │ │ - 相关性分析    │ │ │ - 相关性分析结果                           │ │   │ │
│ │ │ │ - 回归分析      │ │ │ - 回归分析结果                             │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ 对比分析设置     │ │ │ 对比分析区域                               │ │   │ │
│ │ │ │ - 方法对比      │ │ │ - 方法性能对比                             │ │   │ │
│ │ │ │ - 参数对比      │ │ │ - 精度对比图                               │ │   │ │
│ │ │ │ - 时间对比      │ │ │ - 收敛性对比                               │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ └─────────────────────┴─────────────────────────────────────────────────┘   │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────────────────────┐ │
│ │ 页面5: 数据管理与导出 (Data Management & Export)                           │ │
│ │ ┌─────────────────────┬─────────────────────────────────────────────────┐   │ │
│ │ │ 左侧: 数据管理面板   │ 右侧: 导出配置区域                             │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ 历史记录区域     │ │ │ 导出设置区域                               │ │   │ │
│ │ │ │ - 结果历史      │ │ │ - 格式选择                                 │ │   │ │
│ │ │ │ - 快速加载      │ │ │ - 质量设置                                 │ │   │ │
│ │ │ │ - 批量操作      │ │ │ - 路径设置                                 │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ │ ┌─────────────────┐ │ ┌─────────────────────────────────────────────┐ │   │ │
│ │ │ │ 项目管理区域     │ │ │ 导出预览区域                               │ │   │ │
│ │ │ │ - 项目保存      │ │ │ - 预览图表                                 │ │   │ │
│ │ │ │ - 项目加载      │ │ │ - 文件列表                                 │ │   │ │
│ │ │ │ - 模板管理      │ │ │ - 导出进度                                 │ │   │ │
│ │ │ └─────────────────┘ │ └─────────────────────────────────────────────┘ │   │ │
│ │ └─────────────────────┴─────────────────────────────────────────────────┘   │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────────────────┤
│ 状态栏: 就绪 | 样本数: 0 | 当前方法: 无 | 进度: 0% | 内存使用: 0MB | 时间: 00:00:00 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 🎨 详细功能设计

### **页面1: 数据准备 (Data Preparation)**

#### **1.1 数据输入面板**
```python
class DataInputPanel(QWidget):
    """数据输入面板"""
    
    def __init__(self):
        # 输入方式选择
        self.input_method_group = QButtonGroup()
        self.file_input_radio = QRadioButton("文件选择")
        self.custom_input_radio = QRadioButton("自定义输入")
        
        # 文件选择区域
        self.file_selection_area = FileSelectionArea()
        
        # 自定义输入区域
        self.custom_input_area = CustomInputArea()
        
        # 布局
        self.setup_layout()

class FileSelectionArea(QWidget):
    """文件选择区域"""
    
    def __init__(self):
        # 文件管理器按钮
        self.file_manager_button = QPushButton("选择文件...")
        self.file_path_label = QLabel("未选择文件")
        
        # 支持的文件格式
        self.supported_formats = [
            "Excel文件 (*.xlsx *.xls)",
            "CSV文件 (*.csv *.txt)",
            "JSON文件 (*.json)",
            "HDF5文件 (*.h5 *.hdf5)",
            "所有支持的文件 (*.xlsx *.xls *.csv *.txt *.json *.h5 *.hdf5)"
        ]
        
        # 文件信息显示
        self.file_info_label = QLabel()
        
        # 连接信号
        self.file_manager_button.clicked.connect(self.open_file_dialog)

class CustomInputArea(QWidget):
    """自定义输入区域"""
    
    def __init__(self):
        # 维度设置
        self.dimension_spinbox = QSpinBox()
        self.dimension_spinbox.setRange(2, 100)  # 支持任意维度
        self.dimension_spinbox.setValue(2)
        
        # Excel编辑器
        self.excel_editor = QTableWidget()
        
        # 数据输入控制
        self.add_sample_button = QPushButton("添加样本")
        self.remove_sample_button = QPushButton("删除样本")
        self.clear_data_button = QPushButton("清空数据")
        
        # 连接信号
        self.dimension_spinbox.valueChanged.connect(self.update_table_size)
        self.add_sample_button.clicked.connect(self.add_sample)
        self.remove_sample_button.clicked.connect(self.remove_sample)
        self.clear_data_button.clicked.connect(self.clear_data)

class DataPreviewArea(QWidget):
    """数据预览区域"""
    
    def __init__(self):
        # 预览控制
        self.preview_button = QPushButton("数据预览")
        self.preview_status_label = QLabel("未预览")
        
        # 数据表格
        self.data_table = QTableWidget()
        self.data_table.setAlternatingRowColors(True)
        
        # 数据统计信息
        self.stats_label = QLabel()
        
        # 连接信号
        self.preview_button.clicked.connect(self.preview_data)
```

#### **1.2 数据预览功能**
```python
class DataPreviewManager:
    """数据预览管理器"""
    
    def __init__(self):
        self.current_data = None
        self.current_file_path = None
        self.current_file_type = None
    
    def load_file(self, file_path):
        """加载文件数据"""
        try:
            if file_path.endswith(('.xlsx', '.xls')):
                return self._load_excel(file_path)
            elif file_path.endswith(('.csv', '.txt')):
                return self._load_csv(file_path)
            elif file_path.endswith('.json'):
                return self._load_json(file_path)
            elif file_path.endswith(('.h5', '.hdf5')):
                return self._load_hdf5(file_path)
            else:
                raise ValueError(f"不支持的文件格式: {file_path}")
        except Exception as e:
            self._show_error_dialog(f"文件加载失败: {str(e)}")
            return None
    
    def validate_data_format(self, data, expected_dimension):
        """验证数据格式"""
        if data is None:
            return False, "数据为空"
        
        # 检查数据形状
        if data.ndim != 2:
            return False, f"数据维度错误: 期望2维，实际{data.ndim}维"
        
        # 检查行数是否匹配
        expected_rows = expected_dimension ** 2
        if data.shape[0] != expected_rows:
            return False, f"测量基数量错误: 期望{expected_rows}个，实际{data.shape[0]}个"
        
        # 检查数据范围
        if np.any(data < 0) or np.any(data > 1):
            return False, "数据值超出[0,1]范围"
        
        # 检查每列是否归一化
        column_sums = np.sum(data, axis=0)
        if not np.allclose(column_sums, 1.0, atol=1e-6):
            return False, "数据未归一化"
        
        return True, "数据格式正确"
    
    def _show_error_dialog(self, message):
        """显示错误对话框"""
        QMessageBox.critical(None, "错误", message)
```

### **页面2: 参数配置与执行监控 (Configuration & Execution Monitoring)**

#### **2.1 参数配置面板**
```python
class ParameterConfigPanel(QWidget):
    """参数配置面板"""
    
    def __init__(self):
        # 基本参数组
        self.basic_params_group = BasicParametersGroup()
        
        # 高级参数组
        self.advanced_params_group = AdvancedParametersGroup()
        
        # 输出设置组
        self.output_settings_group = OutputSettingsGroup()
        
        # 布局
        self.setup_layout()

class BasicParametersGroup(QGroupBox):
    """基本参数组"""
    
    def __init__(self):
        super().__init__("基本参数")
        
        # 系统维度 (自定义输入)
        self.dimension_label = QLabel("系统维度:")
        self.dimension_spinbox = QSpinBox()
        self.dimension_spinbox.setRange(2, 100)
        self.dimension_spinbox.setValue(2)
        
        # 重构方法选择
        self.method_label = QLabel("重构方法:")
        self.linear_checkbox = QCheckBox("线性重构")
        self.mle_checkbox = QCheckBox("MLE重构")
        self.both_checkbox = QCheckBox("两种方法")
        
        # 设置默认值
        self.linear_checkbox.setChecked(True)
        self.mle_checkbox.setChecked(True)
        
        # 布局
        self.setup_layout()

class AdvancedParametersGroup(QGroupBox):
    """高级参数组"""
    
    def __init__(self):
        super().__init__("高级参数")
        
        # 数值容差
        self.tolerance_label = QLabel("数值容差:")
        self.tolerance_spinbox = QDoubleSpinBox()
        self.tolerance_spinbox.setRange(1e-12, 1e-6)
        self.tolerance_spinbox.setValue(1e-9)
        self.tolerance_spinbox.setDecimals(12)
        
        # 迭代次数
        self.max_iterations_label = QLabel("最大迭代次数:")
        self.max_iterations_spinbox = QSpinBox()
        self.max_iterations_spinbox.setRange(100, 10000)
        self.max_iterations_spinbox.setValue(2000)
        
        # 正则化参数
        self.regularization_label = QLabel("正则化参数:")
        self.regularization_spinbox = QDoubleSpinBox()
        self.regularization_spinbox.setRange(0, 1e-2)
        self.regularization_spinbox.setValue(1e-6)
        self.regularization_spinbox.setDecimals(8)
        
        # 投影算子缓存
        self.cache_projectors_checkbox = QCheckBox("缓存投影算子")
        self.cache_projectors_checkbox.setChecked(True)
        
        # 布局
        self.setup_layout()

class OutputSettingsGroup(QGroupBox):
    """输出设置组"""
    
    def __init__(self):
        super().__init__("输出设置")
        
        # 输出目录
        self.output_dir_label = QLabel("输出目录:")
        self.output_dir_edit = QLineEdit()
        self.output_dir_button = QPushButton("浏览...")
        
        # 文件格式
        self.format_label = QLabel("文件格式:")
        self.format_combo = QComboBox()
        self.format_combo.addItems(["JSON", "CSV", "HDF5"])
        
        # 自动保存
        self.auto_save_checkbox = QCheckBox("自动保存结果")
        self.auto_save_checkbox.setChecked(True)
        
        # 布局
        self.setup_layout()
```

#### **2.2 执行监控面板**
```python
class ExecutionMonitoringPanel(QWidget):
    """执行监控面板"""
    
    def __init__(self):
        # 执行控制区域
        self.execution_control_area = ExecutionControlArea()
        
        # 监控面板区域
        self.monitoring_area = MonitoringArea()
        
        # 实时日志区域
        self.log_area = LogArea()
        
        # 布局
        self.setup_layout()

class ExecutionControlArea(QWidget):
    """执行控制区域"""
    
    def __init__(self):
        # 控制按钮
        self.start_button = QPushButton("开始重构")
        self.pause_button = QPushButton("暂停")
        self.resume_button = QPushButton("恢复")
        self.stop_button = QPushButton("停止")
        self.cancel_button = QPushButton("取消")
        
        # 设置按钮样式
        self.start_button.setStyleSheet("QPushButton { background-color: #4CAF50; color: white; }")
        self.stop_button.setStyleSheet("QPushButton { background-color: #f44336; color: white; }")
        self.cancel_button.setStyleSheet("QPushButton { background-color: #ff9800; color: white; }")
        
        # 初始状态
        self.pause_button.setEnabled(False)
        self.resume_button.setEnabled(False)
        self.stop_button.setEnabled(False)
        self.cancel_button.setEnabled(False)
        
        # 连接信号
        self.start_button.clicked.connect(self.start_reconstruction)
        self.pause_button.clicked.connect(self.pause_reconstruction)
        self.resume_button.clicked.connect(self.resume_reconstruction)
        self.stop_button.clicked.connect(self.stop_reconstruction)
        self.cancel_button.clicked.connect(self.cancel_reconstruction)

class MonitoringArea(QWidget):
    """监控面板区域"""
    
    def __init__(self):
        # 整体进度条
        self.overall_progress_bar = QProgressBar()
        self.overall_progress_bar.setRange(0, 100)
        
        # 当前阶段显示
        self.current_stage_label = QLabel("就绪")
        
        # 样本进度显示
        self.sample_progress_label = QLabel("样本: 0/0")
        
        # 时间估算显示
        self.time_estimate_label = QLabel("预计剩余时间: --:--:--")
        
        # 状态信息
        self.status_info_label = QLabel("状态: 就绪")
        
        # 布局
        self.setup_layout()

class LogArea(QWidget):
    """实时日志区域"""
    
    def __init__(self):
        # 日志显示
        self.log_text = QTextEdit()
        self.log_text.setReadOnly(True)
        self.log_text.setMaximumHeight(200)
        
        # 日志控制
        self.clear_log_button = QPushButton("清空日志")
        self.save_log_button = QPushButton("保存日志")
        
        # 连接信号
        self.clear_log_button.clicked.connect(self.clear_log)
        self.save_log_button.clicked.connect(self.save_log)
```

### **页面3: 结果可视化与基础分析 (Results Visualization & Basic Analysis)**

#### **3.1 结果选择面板**
```python
class ResultsSelectionPanel(QWidget):
    """结果选择面板"""
    
    def __init__(self):
        # 结果列表
        self.results_list = QListWidget()
        
        # 筛选控制
        self.method_filter = QComboBox()
        self.method_filter.addItems(["所有方法", "线性重构", "MLE重构"])
        
        # 排序控制
        self.sort_combo = QComboBox()
        self.sort_combo.addItems(["按时间", "按纯度", "按迹", "按样本"])
        
        # 连接信号
        self.results_list.itemSelectionChanged.connect(self.on_result_selected)
        self.method_filter.currentTextChanged.connect(self.filter_results)
        self.sort_combo.currentTextChanged.connect(self.sort_results)

class BasicMetricsPanel(QWidget):
    """基础指标面板"""
    
    def __init__(self):
        # 纯度指标
        self.purity_label = QLabel("纯度: --")
        self.purity_progress = QProgressBar()
        self.purity_progress.setRange(0, 100)
        
        # 迹指标
        self.trace_label = QLabel("迹: --")
        self.trace_progress = QProgressBar()
        self.trace_progress.setRange(0, 100)
        
        # 矩阵秩
        self.rank_label = QLabel("矩阵秩: --")
        
        # 条件数
        self.condition_number_label = QLabel("条件数: --")
        
        # 布局
        self.setup_layout()

class FidelityAnalysisPanel(QWidget):
    """保真度分析面板"""
    
    def __init__(self):
        # 参考文件选择
        self.reference_file_button = QPushButton("选择参考密度矩阵")
        self.reference_file_label = QLabel("未选择参考文件")
        
        # 分析控制
        self.analyze_button = QPushButton("开始分析")
        self.analyze_button.setEnabled(False)
        
        # 结果显示
        self.fidelity_value_label = QLabel("保真度: --")
        self.analysis_status_label = QLabel("状态: 未分析")
        
        # 连接信号
        self.reference_file_button.clicked.connect(self.select_reference_file)
        self.analyze_button.clicked.connect(self.analyze_fidelity)
```

### **页面4: 高级分析功能 (Advanced Analysis Functions)**

#### **4.1 Bell态分析配置**
```python
class BellAnalysisConfigPanel(QWidget):
    """Bell态分析配置面板"""
    
    def __init__(self):
        # Bell态分析开关
        self.bell_analysis_checkbox = QCheckBox("启用Bell态分析")
        
        # 分析参数
        self.bell_parameters_group = QGroupBox("Bell态分析参数")
        
        # 输出格式设置
        self.output_format_group = QGroupBox("输出格式设置")
        
        # 布局
        self.setup_layout()

class StatisticalAnalysisConfigPanel(QWidget):
    """统计分析方法配置面板"""
    
    def __init__(self):
        # 描述性统计
        self.descriptive_stats_checkbox = QCheckBox("描述性统计")
        self.descriptive_stats_checkbox.setChecked(True)
        
        # 假设检验
        self.hypothesis_test_checkbox = QCheckBox("假设检验")
        
        # 相关性分析
        self.correlation_analysis_checkbox = QCheckBox("相关性分析")
        
        # 回归分析
        self.regression_analysis_checkbox = QCheckBox("回归分析")
        
        # 布局
        self.setup_layout()
```

### **页面5: 数据管理与导出 (Data Management & Export)**

#### **5.1 数据管理面板**
```python
class DataManagementPanel(QWidget):
    """数据管理面板"""
    
    def __init__(self):
        # 历史记录区域
        self.history_area = HistoryArea()
        
        # 项目管理区域
        self.project_area = ProjectArea()
        
        # 布局
        self.setup_layout()

class HistoryArea(QWidget):
    """历史记录区域"""
    
    def __init__(self):
        # 历史记录列表
        self.history_list = QListWidget()
        
        # 操作按钮
        self.load_button = QPushButton("加载")
        self.delete_button = QPushButton("删除")
        self.clear_button = QPushButton("清空")
        
        # 连接信号
        self.load_button.clicked.connect(self.load_selected)
        self.delete_button.clicked.connect(self.delete_selected)
        self.clear_button.clicked.connect(self.clear_history)

class ProjectArea(QWidget):
    """项目管理区域"""
    
    def __init__(self):
        # 项目操作
        self.save_project_button = QPushButton("保存项目")
        self.load_project_button = QPushButton("加载项目")
        self.new_project_button = QPushButton("新建项目")
        
        # 模板管理
        self.save_template_button = QPushButton("保存为模板")
        self.load_template_button = QPushButton("加载模板")
        
        # 连接信号
        self.save_project_button.clicked.connect(self.save_project)
        self.load_project_button.clicked.connect(self.load_project)
        self.new_project_button.clicked.connect(self.new_project)
```

## 🎯 关键特性总结

### **1. 用户友好的数据输入**
- 支持文件管理器选择文件
- 支持多种文件格式 (Excel/CSV/JSON/HDF5)
- 支持自定义输入和Excel编辑器
- 自动格式验证和错误提示

### **2. 灵活的维度支持**
- 支持任意维度 (2-100)
- 自定义维度输入
- 自动维度推断

### **3. 专业的参数配置**
- 基本参数和高级参数分离
- 实时参数验证
- 参数预设和模板

### **4. 完整的执行监控**
- 实时进度显示
- 状态信息监控
- 日志记录和查看
- 执行控制 (开始/暂停/停止/取消)

### **5. 丰富的可视化功能**
- 多种图表类型
- 交互式可视化
- 实时更新
- 导出功能

### **6. 高级分析功能**
- Bell态分析
- 统计分析
- 对比分析
- 保真度分析

### **7. 完善的数据管理**
- 历史记录管理
- 项目管理
- 模板管理
- 批量操作

这个重新设计的方案完全符合您的需求，提供了专业、完整、用户友好的GUI界面！
